---
phase: 01-foundation-and-api-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - app/layout.tsx
  - app/page.tsx
  - vitest.config.mts
  - tests/env.test.ts
  - tests/validation.test.ts
  - tests/api.test.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - INPUT-06

must_haves:
  truths:
    - "Next.js 16 development server starts successfully"
    - "TypeScript compiles with strict mode enabled"
    - "Test framework runs and reports results"
    - "Application has no database or session dependencies"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next@16.2.6"
    - path: "tsconfig.json"
      provides: "TypeScript configuration with strict mode"
      contains: "strict: true"
    - path: "vitest.config.mts"
      provides: "Vitest test configuration"
      contains: "jsdom"
    - path: "tests/env.test.ts"
      provides: "Environment validation tests"
      min_lines: 20
    - path: "tests/validation.test.ts"
      provides: "Zod schema validation tests"
      min_lines: 30
    - path: "tests/api.test.ts"
      provides: "Stateless architecture tests"
      min_lines: 15
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "pnpm install"
      pattern: "pnpm install"
    - from: "vitest.config.mts"
      to: "tests/*.test.ts"
      via: "test runner"
      pattern: "pnpm test"
---

<objective>
Initialize Next.js 16 project with TypeScript, Tailwind v4, and Vitest testing framework. Establishes foundational project structure and test infrastructure that all subsequent phases build upon.

Purpose: Create clean, type-safe foundation with automated testing before any feature implementation begins.

Output: Working Next.js 16 application with dev server, test framework, and strict TypeScript configuration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-api-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-api-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 Project</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, tailwind.config.ts, app/layout.tsx, app/page.tsx, .gitignore, .eslintrc.json</files>
  <action>
Initialize Next.js 16.2.6 project using create-next-app with TypeScript and Tailwind v4.3 (per user decision from CONTEXT.md).

Commands:
```bash
pnpm create next-app@latest . --typescript --tailwind --app --use-pnpm --eslint --no-src-dir
```

After initialization:
1. Update tsconfig.json to enable strict mode and noUncheckedIndexedAccess:
   - Set "strict": true
   - Set "noUncheckedIndexedAccess": true
   - Verify "paths": {"@/*": ["./*"]} exists for path aliases

2. Install Zod for validation (per RESEARCH.md standard stack):
   ```bash
   pnpm install zod@4.4.3
   ```

3. Clean up default page.tsx to minimal placeholder:
   ```typescript
   export default function Home() {
     return (
       <main className="flex min-h-screen flex-col items-center justify-center p-24">
         <h1 className="text-4xl font-bold">AI Cold Outreach Personalizer</h1>
         <p className="mt-4 text-lg text-gray-600">Foundation phase - UI coming in Phase 3</p>
       </main>
     )
   }
   ```

4. Verify dev server starts: `pnpm dev`
5. Verify TypeScript compiles: `pnpm run build`

Note: Using pnpm (not npm) per user decision - 53% faster, 50% less disk space, 2025-2026 Next.js standard.
  </action>
  <verify>
    <automated>pnpm run build && pnpm dev --turbopack</automated>
  </verify>
  <done>
- Next.js 16.2.6 installed with TypeScript and Tailwind v4.3
- tsconfig.json has strict mode enabled
- Dev server starts without errors
- Build completes successfully
- Zod 4.4.3 installed and listed in package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Setup Vitest Testing Framework</name>
  <files>vitest.config.mts, tests/env.test.ts, tests/validation.test.ts, tests/api.test.ts, package.json</files>
  <action>
Install and configure Vitest 4.1 with React Testing Library for automated testing (per RESEARCH.md validation architecture).

1. Install Vitest and testing dependencies:
   ```bash
   pnpm add -D vitest@4.1 @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom vite-tsconfig-paths
   ```

2. Create vitest.config.mts in project root:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import tsconfigPaths from 'vite-tsconfig-paths'

   export default defineConfig({
     plugins: [react(), tsconfigPaths()],
     test: {
       environment: 'jsdom',
       globals: true,
       setupFiles: []
     }
   })
   ```

3. Add test script to package.json:
   ```json
   {
     "scripts": {
       "test": "vitest",
       "test:run": "vitest --run"
     }
   }
   ```

4. Create test scaffold files (per RESEARCH.md Wave 0 gaps):

   **tests/env.test.ts** (covers INFRA-03):
   ```typescript
   import { describe, it, expect } from 'vitest'

   describe('Environment Validation', () => {
     it('should validate ANTHROPIC_API_KEY format', () => {
       // Test will be implemented when types/env.ts is created in Plan 02
       expect(true).toBe(true)
     })
   })
   ```

   **tests/validation.test.ts** (covers INPUT-06):
   ```typescript
   import { describe, it, expect } from 'vitest'

   describe('Zod Schema Validation', () => {
     it('should validate prospect input fields', () => {
       // Test will be implemented when types/prospect.ts is created in Plan 02
       expect(true).toBe(true)
     })
   })
   ```

   **tests/api.test.ts** (covers INFRA-01):
   ```typescript
   import { describe, it, expect } from 'vitest'

   describe('Stateless Architecture', () => {
     it('should have no database dependencies', () => {
       // Verify package.json has no database packages
       const packageJson = require('../package.json')
       const deps = Object.keys(packageJson.dependencies || {})
       const devDeps = Object.keys(packageJson.devDependencies || {})
       const allDeps = [...deps, ...devDeps]

       const dbPackages = ['prisma', 'mongoose', 'pg', 'mysql', 'sqlite', '@prisma/client']
       const hasDb = allDeps.some(dep => dbPackages.includes(dep))

       expect(hasDb).toBe(false)
     })
   })
   ```

5. Verify tests run: `pnpm test --run`

Note: Test scaffolds are minimal stubs. Plan 02 will add real validation tests when type definitions exist.
  </action>
  <verify>
    <automated>pnpm test --run</automated>
  </verify>
  <done>
- Vitest 4.1 and testing libraries installed
- vitest.config.mts exists with jsdom environment
- Test scaffold files created (env.test.ts, validation.test.ts, api.test.ts)
- `pnpm test` command runs successfully
- INFRA-01 test passes (no database dependencies)
  </done>
</task>

</tasks>

<verification>
Run full test suite to verify foundation:
```bash
pnpm test --run
pnpm run build
pnpm dev
```

All commands should complete without errors. Visit http://localhost:3000 to confirm Next.js server is running.
</verification>

<success_criteria>
- Next.js 16.2.6 development server starts successfully on http://localhost:3000
- TypeScript compiles with strict mode enabled, no errors
- Vitest test framework runs and reports green status
- Package.json lists Next.js 16.2.6, Zod 4.4.3, Vitest 4.1, pnpm as package manager
- Build completes in under 30 seconds (Turbopack performance)
- Project has no database dependencies (INFRA-01 requirement verified)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-api-contracts/01-01-SUMMARY.md` documenting:
- Next.js version and configuration
- Installed dependencies and versions
- TypeScript strictness settings
- Test framework configuration
- Any deviations from plan or issues encountered
</output>
